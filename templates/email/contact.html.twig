<h1>A l'attention du service "{{ contact.division.name }}"</h1>

<table>
    <tr>
        <td>Nom :</td>
        <td>{{ contact.lastname }}</td>
    </tr>
    <tr>
        <td>Prénom :</td>
        <td>{{ contact.firstname }}</td>
    </tr>
    <tr>
        <td>E-mail :</td>
        <td>{{ contact.email }}</td>
    </tr>
</table>

<div>
    <p>{{ contact.message }}</p>
</div>